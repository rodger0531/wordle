export enum DigitStyle {
  PRESENT = "present",
  CORRECT = "correct",
  EMPTY = "empty",
  ABSENT = "absent",
}

export enum GameState {
  PLAYING = "PLAYING",
  FINISHED = "FINISHED",
}

export const ALLOWED_GUESSES = 6;
export const WORD_LENGTH = 5;
